research_model: opus
plan_model: opus
execute_model: sonnet

source:
  type: github
  repo: GrueneAT/leitfaden
  sync_on_create: true

github:
  repo: GrueneAT/leitfaden

naming:
  branch: "issue/{slug}"

commits:
  format: conventional
  prefix: true

commit_artifacts: true
worktrees: true
worktree_dir: ".worktrees"
staleness_days: 7

repos:
  - name: leitfaden
    path: .
