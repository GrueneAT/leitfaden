{
  "name": "ai-build-guide",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Guide for building small browser-only tools with an AI assistant",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "index": "pagefind --site dist",
    "postbuild": "pagefind --site dist"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "pagefind": "^1.5.2",
    "typescript": "^5.9.3"
  }
}
